@model IEnumerable<BoekhoudAssistent.Models.BSEG>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BUZID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AUGDT)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AUGCP)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AUGBL)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BSCHL)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BUZID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AUGDT)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AUGCP)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AUGBL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BSCHL)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
